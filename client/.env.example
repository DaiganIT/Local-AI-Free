# URL of the relay server (default: http://localhost:3000)
VITE_RELAY_URL=http://localhost:3000

# API key for the relay server (optional, must match SERVER_API_KEYS on the server)
VITE_RELAY_API_KEY=